/**
 * Enum
 */
// enum Keylist {keyOn,keyTwo,keyThree}
// let newKey: Keylist = Keylist.keyOn;
//
// console.log(newKey);

const enum Directions {
    Up,
    Down,
    Left,
    Right
}

let directions = [Directions.Up, Directions.Down, Directions.Left, Directions.Right]


